{
  "description": "Keep API and Appium repairs on one exact queued test until fresh JUnit proof exists.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Bash$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$(git rev-parse --show-toplevel)/.agents/hooks/test_repair.py\" before --adapter codex",
            "commandWindows": "powershell -NoProfile -Command \"$root = git rev-parse --show-toplevel; & python (Join-Path $root '.agents\\hooks\\test_repair.py') before --adapter codex\"",
            "timeout": 10,
            "statusMessage": "Checking the exact-test repair lock"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "^Bash$",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$(git rev-parse --show-toplevel)/.agents/hooks/test_repair.py\" after --adapter codex",
            "commandWindows": "powershell -NoProfile -Command \"$root = git rev-parse --show-toplevel; & python (Join-Path $root '.agents\\hooks\\test_repair.py') after --adapter codex\"",
            "timeout": 10,
            "statusMessage": "Recording targeted test evidence"
          }
        ]
      }
    ]
  }
}
